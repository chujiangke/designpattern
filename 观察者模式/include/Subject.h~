#ifndef SUBJECT_H
#define SUBJECT_H

#include <iostream>
#include <string>
using namespace std;
class Subject
{
 public:
  virtual void Attach(Observer *) = 0;
  virtual void Deatch(Observer *) = 0;
  
};
#endif
